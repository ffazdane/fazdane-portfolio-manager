.env
.streamlit/secrets.toml
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.venv/
pip-log.txt
pip-delete-this-directory.txt
.DS_Store

# Database files — never commit the live database
*.db
*.db-shm
*.db-wal

# Import files — persistent data, not code — never commit
data/imports/*
!data/imports/.gitkeep

# Local DB backups — machine-specific, not code
data/backups/
